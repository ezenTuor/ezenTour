<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="config.mybatis.mapper.oracle.review">
	<insert id="insertReview" parameterType="reviewVo">
		<selectKey keyProperty="review_no" resultType="int" order="BEFORE">
			select REVIEW_seq.nextval from dual
		</selectKey>
		insert into review(review_no, user_no, history_no, title, content, score)
		values(#{review_no}, #{user_no}, #{history_no}, #{title}, #{content}, #{score})
	</insert>
</mapper>